<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.datnt.groupexpense">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Acitivity.GroupMemberActivity"></activity>
        <activity
            android:name=".Acitivity.ExpandManagementAcitivity"
            android:label="@string/title_activity_expand_management_acitivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".Acitivity.ViewInvitationsActivity" />
        <activity android:name=".Acitivity.CreateGroupActivity" />
        <activity android:name=".Acitivity.ExpenseActivity" />
        <activity android:name=".Acitivity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>